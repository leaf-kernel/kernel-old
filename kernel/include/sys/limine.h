#ifndef __LIMINE_LEAF_H__
#define __LIMINE_LEAF_H__

#if !defined(LEAF_LIMINE)
#error "Leaf currently only supports x86_64 with limine!"
#else
#include <limine/limine.h>
#endif

#endif  // __LIMINE_LEAF_H__
